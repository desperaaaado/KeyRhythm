# Database migrations

The current prototype stores ordered, explicit migrations in `keyrhythm.library.database.MIGRATIONS`. This directory is reserved for future migrations that are too large to keep inline.

